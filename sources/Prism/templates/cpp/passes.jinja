// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by Prism from .prism files in sources/Prism/defs/
// Changes will be lost on next generation. Edit the .prism source files instead.
// ============================================================================
export module FrameGraph:Passes;

import :Base;
import :ViewportContext;
import Core;
import HAL;

export import "../defines.h";

{% for pass in parsed.passes %}
#include "pass/{{parsed.passes[pass].ns_dir}}{{pass}}.h"
{%- endfor %}
{%- for view in parsed.views %}
#include "pass/{{parsed.views[view].ns_dir}}{{view}}.h"
{%- endfor %}
{%- for pipeline in parsed.pipelines %}
#include "pass/{{parsed.pipelines[pipeline].ns_dir}}{{pipeline}}.pipeline.h"
{%- endfor %}

export namespace Passes
{
{#- Iterating a map yields its keys (names), and jinja2cpp returns the string
    itself for any attribute of a string -- so look each one up. -#}
{%- for name in parsed.passes %}
{%- set pass = parsed.passes[name] %}
	{{ pass.ns_open }}using ::Passes::{{pass.qn}};{{ pass.ns_braces }}
{%- endfor %}
}

{%- if parsed.pipelines %}

export namespace Pipelines
{
{%- for name in parsed.pipelines %}
{%- set pipeline = parsed.pipelines[name] %}
	{{ pipeline.ns_open }}using ::Pipelines::{{pipeline.qn}};{{ pipeline.ns_braces }}
{%- endfor %}
}
{%- endif %}
