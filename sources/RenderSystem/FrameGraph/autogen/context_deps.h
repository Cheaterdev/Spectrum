// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by SigParser from .sig files in sources/SIGParser/sigs/
// Changes will be lost on next generation. Edit the .sig source files instead.
// ============================================================================
// Which Table:: context fields each pass's generated setup actually reads.
//
// Extracted from the parsed [SetupCondition]/[RenderCondition]/[Optional]
// expressions themselves, not from a hand-maintained list -- add a condition
// naming a new field and the masks below widen on the next generation. A
// hand-kept version of this table is the one thing that must not exist: a
// dependency missed there produces a cache that silently serves a stale graph,
// which surfaces as a corrupted barrier many frames later rather than as an
// error at the point of the mistake.
//
// The intended use is a per-frame cache key. If no field in a pass's mask
// changed since the previous frame, that pass's setup() cannot return a
// different answer, so everything derived from it -- the enabled set, the
// dependency edges, the sync/fence chains -- is still valid and can be reused
// rather than recomputed.
#pragma once
// This header is included from a global module fragment (FrameGraph.Base.ixx),
// which is why the #include lives here and why the header must not be moved
// below an `export module` line -- a #include in a module purview would attach
// <bitset> to that module.
#include <bitset>
#include "pass_ids.h"

namespace FrameGraph
{

// Fingerprint of this generated ID space (every PassID and ResourceID name, in
// enum order). A serialized graph plan stores passes and resources by ID; if a
// .sig edit renumbers those enums, a plan from before the edit would not fail
// to load, it would misapply -- wrong resource, wrong barriers. A plan whose
// header does not carry this exact value must be rejected.
constexpr unsigned long long generated_id_space_hash = 7986093507123456630ull;

// One bit per context field, in (struct, field) declaration order.
enum class ContextFieldID : unsigned int
{
	DDGISelectors_enabled,
	DDGISelectors_show_probes,
	ViewportContext_frame_size,
	ViewportContext_upscale_size,
	IndirectGISelectors_indirect_source,
	IndirectGISelectors_reflection_source,
	RenderDeviceCapabilities_rtx_supported,
	RenderDeviceCapabilities_dlss_available,
	RenderDeviceCapabilities_dlssrr_available,
	SkyState_prev_sun_dir,
	SkyState_sky_changed,
	StencilState_has_selection,
	UIState_UI_Passes_needed,
	UIRenderState_passes_needed,
	UIRenderState_per_pass,
	UpscalerSelectors_upscaler_type,
	UpscalerSelectors_upscaling_enabled,
	VoxelGISelectors_voxelize_scene,
	VoxelGISelectors_light_scene,
	VoxelGISelectors_reflection_enabled,
	VoxelGISelectors_debug_voxel_trace,
	VSMSelectors_use_vsm_penumbra,
	VSMSelectors_use_vsm_contact_shadow,
	VSMSelectors_vsm_debug_view,
	VSMSelectors_shadow_source,
	Count
};

// Sized to exactly the number of fields, so adding contexts has no ceiling to
// run into -- this deliberately replaces an earlier single uint64 mask, which
// would have silently truncated a pass's dependency set on the 65th field. A
// truncated mask under-reports a dependency, and under-reporting is the one
// failure mode here that produces a silently stale cache rather than a loud
// error.
using ContextFieldMask = std::bitset<(unsigned int)ContextFieldID::Count>;

constexpr ContextFieldMask context_field_bit(ContextFieldID f)
{
	ContextFieldMask m;
	m.set((unsigned int)f);
	return m;
}

// One named single-bit constant per field, so a mask below reads as the
// condition it was extracted from. Derived from ContextFieldID rather than
// hand-numbered: the two can never drift, and inserting a field into a struct
// renumbers the enum without changing the meaning of any mask.
namespace ContextField
{
	constexpr ContextFieldMask None = ContextFieldMask{};
	constexpr ContextFieldMask DDGISelectors_enabled = context_field_bit(ContextFieldID::DDGISelectors_enabled);
	constexpr ContextFieldMask DDGISelectors_show_probes = context_field_bit(ContextFieldID::DDGISelectors_show_probes);
	constexpr ContextFieldMask ViewportContext_frame_size = context_field_bit(ContextFieldID::ViewportContext_frame_size);
	constexpr ContextFieldMask ViewportContext_upscale_size = context_field_bit(ContextFieldID::ViewportContext_upscale_size);
	constexpr ContextFieldMask IndirectGISelectors_indirect_source = context_field_bit(ContextFieldID::IndirectGISelectors_indirect_source);
	constexpr ContextFieldMask IndirectGISelectors_reflection_source = context_field_bit(ContextFieldID::IndirectGISelectors_reflection_source);
	constexpr ContextFieldMask RenderDeviceCapabilities_rtx_supported = context_field_bit(ContextFieldID::RenderDeviceCapabilities_rtx_supported);
	constexpr ContextFieldMask RenderDeviceCapabilities_dlss_available = context_field_bit(ContextFieldID::RenderDeviceCapabilities_dlss_available);
	constexpr ContextFieldMask RenderDeviceCapabilities_dlssrr_available = context_field_bit(ContextFieldID::RenderDeviceCapabilities_dlssrr_available);
	constexpr ContextFieldMask SkyState_prev_sun_dir = context_field_bit(ContextFieldID::SkyState_prev_sun_dir);
	constexpr ContextFieldMask SkyState_sky_changed = context_field_bit(ContextFieldID::SkyState_sky_changed);
	constexpr ContextFieldMask StencilState_has_selection = context_field_bit(ContextFieldID::StencilState_has_selection);
	constexpr ContextFieldMask UIState_UI_Passes_needed = context_field_bit(ContextFieldID::UIState_UI_Passes_needed);
	constexpr ContextFieldMask UIRenderState_passes_needed = context_field_bit(ContextFieldID::UIRenderState_passes_needed);
	constexpr ContextFieldMask UIRenderState_per_pass = context_field_bit(ContextFieldID::UIRenderState_per_pass);
	constexpr ContextFieldMask UpscalerSelectors_upscaler_type = context_field_bit(ContextFieldID::UpscalerSelectors_upscaler_type);
	constexpr ContextFieldMask UpscalerSelectors_upscaling_enabled = context_field_bit(ContextFieldID::UpscalerSelectors_upscaling_enabled);
	constexpr ContextFieldMask VoxelGISelectors_voxelize_scene = context_field_bit(ContextFieldID::VoxelGISelectors_voxelize_scene);
	constexpr ContextFieldMask VoxelGISelectors_light_scene = context_field_bit(ContextFieldID::VoxelGISelectors_light_scene);
	constexpr ContextFieldMask VoxelGISelectors_reflection_enabled = context_field_bit(ContextFieldID::VoxelGISelectors_reflection_enabled);
	constexpr ContextFieldMask VoxelGISelectors_debug_voxel_trace = context_field_bit(ContextFieldID::VoxelGISelectors_debug_voxel_trace);
	constexpr ContextFieldMask VSMSelectors_use_vsm_penumbra = context_field_bit(ContextFieldID::VSMSelectors_use_vsm_penumbra);
	constexpr ContextFieldMask VSMSelectors_use_vsm_contact_shadow = context_field_bit(ContextFieldID::VSMSelectors_use_vsm_contact_shadow);
	constexpr ContextFieldMask VSMSelectors_vsm_debug_view = context_field_bit(ContextFieldID::VSMSelectors_vsm_debug_view);
	constexpr ContextFieldMask VSMSelectors_shadow_source = context_field_bit(ContextFieldID::VSMSelectors_shadow_source);
}

// Fields that ONLY ever feed a resource descriptor ([Size]/[Format]), never a
// pass condition.
//
// This is what lets the two caches be keyed separately. A resource's geometry
// is a pure function of these -- every non-literal [Size] in the sigs reads only
// ViewportContext, and every [Format]/[ArrayCount]/[MipCount] is literal or a
// Constants:: entry -- while the pass set cannot depend on them at all. Folding
// both halves into one key would fork a whole topology plan per window size:
// N enable-set variants x M sizes instead of N + M.
constexpr ContextFieldMask desc_only_fields =
	  ContextField::ViewportContext_frame_size
	| ContextField::ViewportContext_upscale_size;

struct PassContextDeps
{
	PassID   pass;

	// Fields the pass's own enable decision reads. A change here can add or
	// remove passes, so it invalidates the whole dependency graph.
	ContextFieldMask condition_fields;

	// Fields its per-field [Optional] guards read. A change here leaves the
	// pass set alone and only moves which resources an already-enabled pass
	// touches -- a strictly cheaper invalidation, which is why it is tracked
	// separately rather than folded into condition_fields.
	ContextFieldMask optional_fields;

	// False when some part of the expression was a raw backtick span, or named
	// something that is not a SIG-declared context. The mask is then a LOWER
	// BOUND, not the full set: treat the pass as always dirty. Under-reporting
	// a dependency is the only failure mode here that is silent, so it is made
	// explicit rather than assumed away.
	bool     deps_complete;
};

static inline const PassContextDeps pass_context_deps[] = {
	{ PassID::AssetGBuffer,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::AssetMip,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::BlueNoise,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::DDGIProbeSelect,
		  ContextField::DDGISelectors_enabled,
		  ContextField::None,
		true },
	{ PassID::DDGIProbeTrace,
		  ContextField::DDGISelectors_enabled
		| ContextField::RenderDeviceCapabilities_rtx_supported,
		  ContextField::None,
		true },
	{ PassID::DDGIProbeConvolve,
		  ContextField::DDGISelectors_enabled
		| ContextField::RenderDeviceCapabilities_rtx_supported,
		  ContextField::None,
		true },
	{ PassID::DDGIDebug,
		  ContextField::DDGISelectors_enabled
		| ContextField::DDGISelectors_show_probes,
		  ContextField::None,
		true },
	{ PassID::DDGIIndirectDebug,
		  ContextField::DDGISelectors_enabled
		| ContextField::RenderDeviceCapabilities_rtx_supported,
		  ContextField::None,
		true },
	{ PassID::FSR,
		  ContextField::UpscalerSelectors_upscaler_type
		| ContextField::UpscalerSelectors_upscaling_enabled,
		  ContextField::None,
		true },
	{ PassID::ResultCreation,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::NRD_GBufferPack,
		  ContextField::RenderDeviceCapabilities_rtx_supported
		| ContextField::RenderDeviceCapabilities_dlssrr_available
		| ContextField::UpscalerSelectors_upscaler_type,
		  ContextField::IndirectGISelectors_indirect_source
		| ContextField::IndirectGISelectors_reflection_source,
		true },
	{ PassID::NRD_REBLUR_Execute,
		  ContextField::RenderDeviceCapabilities_rtx_supported
		| ContextField::RenderDeviceCapabilities_dlssrr_available
		| ContextField::UpscalerSelectors_upscaler_type,
		  ContextField::None,
		true },
	{ PassID::NRD_SIGMA_Execute,
		  ContextField::RenderDeviceCapabilities_rtx_supported
		| ContextField::RenderDeviceCapabilities_dlssrr_available
		| ContextField::UpscalerSelectors_upscaler_type
		| ContextField::VSMSelectors_use_vsm_penumbra
		| ContextField::VSMSelectors_shadow_source,
		  ContextField::VSMSelectors_shadow_source,
		true },
	{ PassID::NRD_IndirectCombine,
		  ContextField::UpscalerSelectors_upscaler_type,
		  ContextField::None,
		true },
	{ PassID::NRD_ShadowCombine,
		  ContextField::RenderDeviceCapabilities_rtx_supported
		| ContextField::RenderDeviceCapabilities_dlssrr_available
		| ContextField::UpscalerSelectors_upscaler_type
		| ContextField::VSMSelectors_shadow_source,
		  ContextField::None,
		true },
	{ PassID::PSSM_Cascade,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::PSSM_GenerateMask,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::PSSM_Combine,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::PSSM_Global,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::RTXShadow,
		  ContextField::RenderDeviceCapabilities_rtx_supported,
		  ContextField::None,
		true },
	{ PassID::RTXColorPass,
		  ContextField::RenderDeviceCapabilities_rtx_supported,
		  ContextField::None,
		true },
	{ PassID::PreScene,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::Profiler,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::Scene,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::Sky,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::CubeSky,
		  ContextField::SkyState_sky_changed,
		  ContextField::None,
		true },
	{ PassID::CubeMapDownsample,
		  ContextField::SkyState_sky_changed,
		  ContextField::None,
		true },
	{ PassID::CubeMapEnviromentProcessor,
		  ContextField::SkyState_sky_changed,
		  ContextField::None,
		true },
	{ PassID::SMAA,
		  ContextField::UpscalerSelectors_upscaling_enabled,
		  ContextField::None,
		true },
	{ PassID::stencil_renderer_before,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::stencil_renderer_after,
		  ContextField::StencilState_has_selection,
		  ContextField::None,
		true },
	{ PassID::UI_PreDraw,
		  ContextField::UIState_UI_Passes_needed,
		  ContextField::None,
		true },
	{ PassID::UI_Render,
		  ContextField::UIRenderState_passes_needed,
		  ContextField::None,
		true },
	{ PassID::AssetPreview,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::UpscalingDLSS,
		  ContextField::UpscalerSelectors_upscaler_type
		| ContextField::UpscalerSelectors_upscaling_enabled,
		  ContextField::None,
		true },
	{ PassID::NormalRoughnessRepack,
		  ContextField::UpscalerSelectors_upscaler_type,
		  ContextField::None,
		true },
	{ PassID::UpscalingDLSSRR,
		  ContextField::UpscalerSelectors_upscaler_type
		| ContextField::UpscalerSelectors_upscaling_enabled,
		  ContextField::None,
		true },
	{ PassID::GBufferDownsampler,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::VoxelDebug,
		  ContextField::VoxelGISelectors_debug_voxel_trace,
		  ContextField::None,
		true },
	{ PassID::ReflectionRTXHalf,
		  ContextField::RenderDeviceCapabilities_rtx_supported
		| ContextField::RenderDeviceCapabilities_dlssrr_available,
		  ContextField::None,
		true },
	{ PassID::ReflectionRTX,
		  ContextField::RenderDeviceCapabilities_rtx_supported
		| ContextField::RenderDeviceCapabilities_dlssrr_available,
		  ContextField::None,
		true },
	{ PassID::ShadowRTX,
		  ContextField::RenderDeviceCapabilities_rtx_supported
		| ContextField::RenderDeviceCapabilities_dlssrr_available,
		  ContextField::None,
		true },
	{ PassID::IndirectRTXHalf,
		  ContextField::RenderDeviceCapabilities_rtx_supported
		| ContextField::RenderDeviceCapabilities_dlssrr_available,
		  ContextField::None,
		true },
	{ PassID::IndirectRTX,
		  ContextField::RenderDeviceCapabilities_rtx_supported
		| ContextField::RenderDeviceCapabilities_dlssrr_available,
		  ContextField::None,
		true },
	{ PassID::ReflCombine,
		  ContextField::UpscalerSelectors_upscaler_type
		| ContextField::VoxelGISelectors_reflection_enabled,
		  ContextField::None,
		true },
	{ PassID::RTXCombine,
		  ContextField::UpscalerSelectors_upscaler_type,
		  ContextField::None,
		true },
	{ PassID::Voxelize,
		  ContextField::VoxelGISelectors_voxelize_scene,
		  ContextField::None,
		true },
	{ PassID::Lighting,
		  ContextField::VoxelGISelectors_light_scene,
		  ContextField::None,
		true },
	{ PassID::Mipmapping,
		  ContextField::VoxelGISelectors_light_scene,
		  ContextField::None,
		true },
	{ PassID::VoxelScreen,
		  ContextField::IndirectGISelectors_indirect_source
		| ContextField::RenderDeviceCapabilities_rtx_supported
		| ContextField::RenderDeviceCapabilities_dlssrr_available
		| ContextField::UpscalerSelectors_upscaler_type,
		  ContextField::None,
		true },
	{ PassID::ScreenReflection,
		  ContextField::IndirectGISelectors_reflection_source
		| ContextField::RenderDeviceCapabilities_rtx_supported
		| ContextField::RenderDeviceCapabilities_dlssrr_available
		| ContextField::UpscalerSelectors_upscaler_type,
		  ContextField::None,
		true },
	{ PassID::VSM_GatherDispatch,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::VSM_RenderPages,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::VSM_HiZRebuild,
		  ContextField::None,
		  ContextField::None,
		true },
	{ PassID::VSM_BlockerClassify,
		  ContextField::VSMSelectors_use_vsm_penumbra,
		  ContextField::None,
		true },
	{ PassID::VSM_BlockerSearch,
		  ContextField::VSMSelectors_use_vsm_penumbra,
		  ContextField::None,
		true },
	{ PassID::VSM_ScreenSpaceShadow,
		  ContextField::VSMSelectors_use_vsm_penumbra
		| ContextField::VSMSelectors_use_vsm_contact_shadow,
		  ContextField::None,
		true },
	{ PassID::VSM_ShadowResolve,
		  ContextField::VSMSelectors_use_vsm_penumbra
		| ContextField::VSMSelectors_shadow_source,
		  ContextField::VSMSelectors_use_vsm_contact_shadow,
		true },
	{ PassID::VSM_Combine,
		  ContextField::VSMSelectors_use_vsm_penumbra
		| ContextField::VSMSelectors_shadow_source,
		  ContextField::VSMSelectors_vsm_debug_view,
		true },
	{ PassID::VSM_DebugClassifyOverlay,
		  ContextField::VSMSelectors_use_vsm_penumbra
		| ContextField::VSMSelectors_vsm_debug_view,
		  ContextField::VSMSelectors_use_vsm_contact_shadow
		| ContextField::VSMSelectors_vsm_debug_view,
		true },
	{ PassID::VSM_DepthAnalysis,
		  ContextField::None,
		  ContextField::None,
		true },
};

static constexpr unsigned int pass_context_deps_count =
	sizeof(pass_context_deps) / sizeof(pass_context_deps[0]);


// ---------------------------------------------------------------------------
// Per-frame snapshot of every context field's value, so "did this pass's inputs
// change?" is answerable without re-running its setup.
//
// One opaque 64-bit encoding per field rather than the field's own type: the
// only question ever asked of it is "same as last frame?", and a uniform array
// keeps the diff a plain loop instead of generated per-field comparisons.
struct ContextSnapshot
{
	unsigned long long values[(unsigned int)ContextFieldID::Count] = {};
};

class Graph;

// Reads every context field out of `graph` into `out`. Generated (see
// autogen/context_snapshot.cpp) because it has to name every Table:: type,
// which is why it is defined in a Graphics-module TU rather than here -- the
// same split, and for the same reason, as PassDefault<T>::setup and
// autogen/pass_defaults.cpp.
void capture_context_snapshot(Graph& graph, ContextSnapshot& out);

// Which fields differ between two snapshots. A set bit means "this field
// changed since last frame", which is what a pass's condition_fields mask is
// tested against.
ContextFieldMask diff_context_snapshots(const ContextSnapshot& a, const ContextSnapshot& b);

// Captures this frame's snapshot into the graph and updates its dirty mask.
// Must run AFTER the pipeline's run_pre_setups() -- [PreSetup] hooks write
// context fields (CubeSky's sun-direction diff is one) -- and before setup().
void update_context_dirty_mask(Graph& graph);

}
