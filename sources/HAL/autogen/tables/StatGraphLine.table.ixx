// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by SigParser from .sig files in sources/SIGParser/sigs/
// Changes will be lost on next generation. Edit the .sig source files instead.
// ============================================================================
export module HAL:Autogen.Tables.StatGraphLine;

import Core;
import :SIG;
import :Types;
import :HLSL;
import :Enums;
export namespace Table
{
	#pragma pack(push, 1)
	struct StatGraphLine
	{
		static constexpr SlotID ID = SlotID::StatGraphLine;
		uint Count;
		uint Width;
		uint Height;
		float Vmin;
		float Vmax;
		float4 LineColor;
		float4 FillColor;
		float4 BgTop;
		float4 BgBot;
		HLSL::StructuredBuffer<float> Samples;
		uint& GetCount() { return Count; }
		uint& GetWidth() { return Width; }
		uint& GetHeight() { return Height; }
		float& GetVmin() { return Vmin; }
		float& GetVmax() { return Vmax; }
		float4& GetLineColor() { return LineColor; }
		float4& GetFillColor() { return FillColor; }
		float4& GetBgTop() { return BgTop; }
		float4& GetBgBot() { return BgBot; }
		HLSL::StructuredBuffer<float>& GetSamples() { return Samples; }
		static constexpr SIG_TYPE TYPE = SIG_TYPE::Table;
		template<class Compiler>
		void compile(Compiler& compiler) const
		{
			compiler.compile(Count);
			compiler.compile(Width);
			compiler.compile(Height);
			compiler.compile(Vmin);
			compiler.compile(Vmax);
			compiler.compile(LineColor);
			compiler.compile(FillColor);
			compiler.compile(BgTop);
			compiler.compile(BgBot);
			compiler.compile(Samples);
		}
		struct Compiled
		{
			uint Count; // uint
			uint Width; // uint
			uint Height; // uint
			float Vmin; // float
			float Vmax; // float
			float4 LineColor; // float4
			float4 FillColor; // float4
			float4 BgTop; // float4
			float4 BgBot; // float4
			uint Samples; // StructuredBuffer<float>

			
			private:
			SERIALIZE()
			{
				ar& NVP(Count);
				ar& NVP(Width);
				ar& NVP(Height);
				ar& NVP(Vmin);
				ar& NVP(Vmax);
				ar& NVP(LineColor);
				ar& NVP(FillColor);
				ar& NVP(BgTop);
				ar& NVP(BgBot);
			}


		};

		static std::string get_typename()
		{
			return "Tables::StatGraphLine";
		}
		private:
		SERIALIZE()
		{
			ar& NVP(Count);
			ar& NVP(Width);
			ar& NVP(Height);
			ar& NVP(Vmin);
			ar& NVP(Vmax);
			ar& NVP(LineColor);
			ar& NVP(FillColor);
			ar& NVP(BgTop);
			ar& NVP(BgBot);
		}

	};
	#pragma pack(pop)
}

