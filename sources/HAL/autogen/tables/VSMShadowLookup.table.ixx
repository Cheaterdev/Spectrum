// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by Prism from .prism files in sources/Prism/defs/
// Changes will be lost on next generation. Edit the .prism source files instead.
// ============================================================================
export module HAL:Autogen.Tables.VSMShadowLookup;

import Core;
import :SIG;
import :Types;
import :HLSL;
import :Enums;
import :Autogen.Tables.Camera;
export namespace Table
{
	#pragma pack(push, 1)
	namespace Shadows
	{
		namespace VSM
		{
			struct VSMShadowLookup
			{
				static constexpr SlotID ID = SlotID::VSMShadowLookup;
				int active_min;
				int active_max;
				int page_size;
				int pages_per_level;
				float4x4 light_view;
				float4 level_info[26];
				HLSL::Texture2DArray<float> vsm_atlas;
				HLSL::Texture2DArray<uint> page_table;
				HLSL::StructuredBuffer<Table::Frame::Camera> page_cameras;
				int& GetActive_min() { return active_min; }
				int& GetActive_max() { return active_max; }
				int& GetPage_size() { return page_size; }
				int& GetPages_per_level() { return pages_per_level; }
				float4x4& GetLight_view() { return light_view; }
				float4* GetLevel_info() { return level_info; }
				HLSL::Texture2DArray<float>& GetVsm_atlas() { return vsm_atlas; }
				HLSL::Texture2DArray<uint>& GetPage_table() { return page_table; }
				HLSL::StructuredBuffer<Table::Frame::Camera>& GetPage_cameras() { return page_cameras; }
				static constexpr SIG_TYPE TYPE = SIG_TYPE::Table;
				template<class Compiler>
				void compile(Compiler& compiler) const
				{
					compiler.compile(active_min, "VSMShadowLookup::active_min");
					compiler.compile(active_max, "VSMShadowLookup::active_max");
					compiler.compile(page_size, "VSMShadowLookup::page_size");
					compiler.compile(pages_per_level, "VSMShadowLookup::pages_per_level");
					compiler.compile(light_view, "VSMShadowLookup::light_view");
					compiler.compile(level_info, "VSMShadowLookup::level_info");
					compiler.compile(vsm_atlas, "VSMShadowLookup::vsm_atlas");
					compiler.compile(page_table, "VSMShadowLookup::page_table");
					compiler.compile(page_cameras, "VSMShadowLookup::page_cameras");
				}
				struct Compiled
				{
					int active_min; // int
					int active_max; // int
					int page_size; // int
					int pages_per_level; // int
					float4x4 light_view; // float4x4
					float4 level_info[26]; // float4
					uint vsm_atlas; // Texture2DArray<float>
					uint page_table; // Texture2DArray<uint>
					uint page_cameras; // StructuredBuffer<Camera>

			
					private:
					SERIALIZE()
					{
						ar& NVP(active_min);
						ar& NVP(active_max);
						ar& NVP(page_size);
						ar& NVP(pages_per_level);
						ar& NVP(light_view);
						ar& NVP(level_info);
					}


				};

				static std::string get_typename()
				{
					return "Tables::VSMShadowLookup";
				}
				private:
				SERIALIZE()
				{
					ar& NVP(active_min);
					ar& NVP(active_max);
					ar& NVP(page_size);
					ar& NVP(pages_per_level);
					ar& NVP(light_view);
					ar& NVP(level_info);
				}

			};
		}
	}
	#pragma pack(pop)
}

