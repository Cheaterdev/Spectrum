// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by Prism from .prism files in sources/Prism/defs/
// Changes will be lost on next generation. Edit the .prism source files instead.
// ============================================================================
export module HAL:Autogen.Tables.VSMScreenSpaceShadowParams;

import Core;
import :SIG;
import :Types;
import :HLSL;
import :Enums;
import :Autogen.Tables.GBuffer;
export namespace Table
{
	#pragma pack(push, 1)
	struct VSMScreenSpaceShadowParams
	{
		static constexpr SlotID ID = SlotID::VSMScreenSpaceShadowParams;
		float4 light_coordinate;
		int2 wave_offset;
		float far_depth_value;
		float near_depth_value;
		float surface_thickness;
		HLSL::Texture2D<float> ambiguous_mask;
		HLSL::RWTexture2D<float> output;
		GBuffer gbuffer;
		HLSL::Texture2D<float>& GetAmbiguous_mask() { return ambiguous_mask; }
		float4& GetLight_coordinate() { return light_coordinate; }
		int2& GetWave_offset() { return wave_offset; }
		float& GetFar_depth_value() { return far_depth_value; }
		float& GetNear_depth_value() { return near_depth_value; }
		float& GetSurface_thickness() { return surface_thickness; }
		HLSL::RWTexture2D<float>& GetOutput() { return output; }
		GBuffer& GetGbuffer() { return gbuffer; }
		static constexpr SIG_TYPE TYPE = SIG_TYPE::Table;
		template<class Compiler>
		void compile(Compiler& compiler) const
		{
			compiler.compile(light_coordinate, "VSMScreenSpaceShadowParams::light_coordinate");
			compiler.compile(wave_offset, "VSMScreenSpaceShadowParams::wave_offset");
			compiler.compile(far_depth_value, "VSMScreenSpaceShadowParams::far_depth_value");
			compiler.compile(near_depth_value, "VSMScreenSpaceShadowParams::near_depth_value");
			compiler.compile(surface_thickness, "VSMScreenSpaceShadowParams::surface_thickness");
			compiler.compile(ambiguous_mask, "VSMScreenSpaceShadowParams::ambiguous_mask");
			compiler.compile(output, "VSMScreenSpaceShadowParams::output");
			compiler.compile(gbuffer, "VSMScreenSpaceShadowParams::gbuffer");
		}
		struct Compiled
		{
			float4 light_coordinate; // float4
			int2 wave_offset; // int2
			float far_depth_value; // float
			float near_depth_value; // float
			float surface_thickness; // float
			uint ambiguous_mask; // Texture2D<float>
			uint output; // RWTexture2D<float>
			GBuffer::Compiled gbuffer; // GBuffer

			
			private:
			SERIALIZE()
			{
				ar& NVP(gbuffer);
				ar& NVP(light_coordinate);
				ar& NVP(wave_offset);
				ar& NVP(far_depth_value);
				ar& NVP(near_depth_value);
				ar& NVP(surface_thickness);
			}


		};

		static std::string get_typename()
		{
			return "Tables::VSMScreenSpaceShadowParams";
		}
		private:
		SERIALIZE()
		{
			ar& NVP(gbuffer);
			ar& NVP(light_coordinate);
			ar& NVP(wave_offset);
			ar& NVP(far_depth_value);
			ar& NVP(near_depth_value);
			ar& NVP(surface_thickness);
		}

	};
	#pragma pack(pop)
}

