// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by Prism from .prism files in sources/Prism/defs/
// Changes will be lost on next generation. Edit the .prism source files instead.
// ============================================================================
export module HAL:Autogen.Tables.Post.BloomTemporal;

import Core;
import :SIG;
import :Types;
import :HLSL;
import :Enums;
export namespace Table
{
	#pragma pack(push, 1)
	namespace Post
	{
		struct BloomTemporal
		{
			static constexpr SlotID ID = SlotID::BloomTemporal;
			float history_weight;
			uint reset;
			HLSL::Texture2D<float4> current;
			HLSL::Texture2D<float4> history;
			HLSL::Texture2D<float> depth;
			HLSL::Texture2D<float2> motion;
			HLSL::RWTexture2D<float4> target;
			HLSL::Texture2D<float4>& GetCurrent() { return current; }
			HLSL::Texture2D<float4>& GetHistory() { return history; }
			HLSL::Texture2D<float>& GetDepth() { return depth; }
			HLSL::Texture2D<float2>& GetMotion() { return motion; }
			HLSL::RWTexture2D<float4>& GetTarget() { return target; }
			float& GetHistory_weight() { return history_weight; }
			uint& GetReset() { return reset; }
			static constexpr SIG_TYPE TYPE = SIG_TYPE::Table;
			template<class Compiler>
			void compile(Compiler& compiler) const
			{
				compiler.compile(history_weight, "BloomTemporal::history_weight");
				compiler.compile(reset, "BloomTemporal::reset");
				compiler.compile(current, "BloomTemporal::current");
				compiler.compile(history, "BloomTemporal::history");
				compiler.compile(depth, "BloomTemporal::depth");
				compiler.compile(motion, "BloomTemporal::motion");
				compiler.compile(target, "BloomTemporal::target");
			}
			struct Compiled
			{
				float history_weight; // float
				uint reset; // uint
				uint current; // Texture2D<float4>
				uint history; // Texture2D<float4>
				uint depth; // Texture2D<float>
				uint motion; // Texture2D<float2>
				uint target; // RWTexture2D<float4>

			
				private:
				SERIALIZE()
				{
					ar& NVP(history_weight);
					ar& NVP(reset);
				}


			};

			static std::string get_typename()
			{
				return "Tables::BloomTemporal";
			}
			private:
			SERIALIZE()
			{
				ar& NVP(history_weight);
				ar& NVP(reset);
			}

		};
	}
	#pragma pack(pop)
}

