// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by Prism from .prism files in sources/Prism/defs/
// Changes will be lost on next generation. Edit the .prism source files instead.
// ============================================================================
export module HAL:Autogen.Tables.Post.LensFlareGhosts;

import Core;
import :SIG;
import :Types;
import :HLSL;
import :Enums;
export namespace Table
{
	#pragma pack(push, 1)
	namespace Post
	{
		struct LensFlareGhosts
		{
			static constexpr SlotID ID = SlotID::LensFlareGhosts;
			float ghost_intensity;
			float halo_intensity;
			float halo_radius;
			float chromatic;
			float aspect;
			HLSL::Texture2D<float4> source;
			HLSL::RWTexture2D<float4> target;
			HLSL::Texture2D<float4>& GetSource() { return source; }
			HLSL::RWTexture2D<float4>& GetTarget() { return target; }
			float& GetGhost_intensity() { return ghost_intensity; }
			float& GetHalo_intensity() { return halo_intensity; }
			float& GetHalo_radius() { return halo_radius; }
			float& GetChromatic() { return chromatic; }
			float& GetAspect() { return aspect; }
			static constexpr SIG_TYPE TYPE = SIG_TYPE::Table;
			template<class Compiler>
			void compile(Compiler& compiler) const
			{
				compiler.compile(ghost_intensity, "LensFlareGhosts::ghost_intensity");
				compiler.compile(halo_intensity, "LensFlareGhosts::halo_intensity");
				compiler.compile(halo_radius, "LensFlareGhosts::halo_radius");
				compiler.compile(chromatic, "LensFlareGhosts::chromatic");
				compiler.compile(aspect, "LensFlareGhosts::aspect");
				compiler.compile(source, "LensFlareGhosts::source");
				compiler.compile(target, "LensFlareGhosts::target");
			}
			struct Compiled
			{
				float ghost_intensity; // float
				float halo_intensity; // float
				float halo_radius; // float
				float chromatic; // float
				float aspect; // float
				uint source; // Texture2D<float4>
				uint target; // RWTexture2D<float4>

			
				private:
				SERIALIZE()
				{
					ar& NVP(ghost_intensity);
					ar& NVP(halo_intensity);
					ar& NVP(halo_radius);
					ar& NVP(chromatic);
					ar& NVP(aspect);
				}


			};

			static std::string get_typename()
			{
				return "Tables::LensFlareGhosts";
			}
			private:
			SERIALIZE()
			{
				ar& NVP(ghost_intensity);
				ar& NVP(halo_intensity);
				ar& NVP(halo_radius);
				ar& NVP(chromatic);
				ar& NVP(aspect);
			}

		};
	}
	#pragma pack(pop)
}

