// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by Prism from .prism files in sources/Prism/defs/
// Changes will be lost on next generation. Edit the .prism source files instead.
// ============================================================================
export module HAL:Autogen.Tables.Post.BloomComposite;

import Core;
import :SIG;
import :Types;
import :HLSL;
import :Enums;
export namespace Table
{
	#pragma pack(push, 1)
	namespace Post
	{
		struct BloomComposite
		{
			static constexpr SlotID ID = SlotID::BloomComposite;
			float2 bloom_texel;
			float radius;
			uint additive;
			float bloom_scale;
			float intensity;
			HLSL::Texture2D<float4> scene;
			HLSL::Texture2D<float4> bloom;
			HLSL::RWTexture2D<float4> target;
			HLSL::Texture2D<float4>& GetScene() { return scene; }
			HLSL::Texture2D<float4>& GetBloom() { return bloom; }
			HLSL::RWTexture2D<float4>& GetTarget() { return target; }
			float2& GetBloom_texel() { return bloom_texel; }
			float& GetRadius() { return radius; }
			uint& GetAdditive() { return additive; }
			float& GetBloom_scale() { return bloom_scale; }
			float& GetIntensity() { return intensity; }
			static constexpr SIG_TYPE TYPE = SIG_TYPE::Table;
			template<class Compiler>
			void compile(Compiler& compiler) const
			{
				compiler.compile(bloom_texel, "BloomComposite::bloom_texel");
				compiler.compile(radius, "BloomComposite::radius");
				compiler.compile(additive, "BloomComposite::additive");
				compiler.compile(bloom_scale, "BloomComposite::bloom_scale");
				compiler.compile(intensity, "BloomComposite::intensity");
				compiler.compile(scene, "BloomComposite::scene");
				compiler.compile(bloom, "BloomComposite::bloom");
				compiler.compile(target, "BloomComposite::target");
			}
			struct Compiled
			{
				float2 bloom_texel; // float2
				float radius; // float
				uint additive; // uint
				float bloom_scale; // float
				float intensity; // float
				uint scene; // Texture2D<float4>
				uint bloom; // Texture2D<float4>
				uint target; // RWTexture2D<float4>

			
				private:
				SERIALIZE()
				{
					ar& NVP(bloom_texel);
					ar& NVP(radius);
					ar& NVP(additive);
					ar& NVP(bloom_scale);
					ar& NVP(intensity);
				}


			};

			static std::string get_typename()
			{
				return "Tables::BloomComposite";
			}
			private:
			SERIALIZE()
			{
				ar& NVP(bloom_texel);
				ar& NVP(radius);
				ar& NVP(additive);
				ar& NVP(bloom_scale);
				ar& NVP(intensity);
			}

		};
	}
	#pragma pack(pop)
}

