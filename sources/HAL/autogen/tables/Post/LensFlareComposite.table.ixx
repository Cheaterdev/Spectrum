// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by Prism from .prism files in sources/Prism/defs/
// Changes will be lost on next generation. Edit the .prism source files instead.
// ============================================================================
export module HAL:Autogen.Tables.Post.LensFlareComposite;

import Core;
import :SIG;
import :Types;
import :HLSL;
import :Enums;
export namespace Table
{
	#pragma pack(push, 1)
	namespace Post
	{
		struct LensFlareComposite
		{
			static constexpr SlotID ID = SlotID::LensFlareComposite;
			float streak_intensity;
			uint use_streaks;
			HLSL::Texture2D<float4> ghosts;
			HLSL::Texture2D<float4> streaks;
			HLSL::RWTexture2D<float4> color;
			HLSL::Texture2D<float4>& GetGhosts() { return ghosts; }
			HLSL::Texture2D<float4>& GetStreaks() { return streaks; }
			HLSL::RWTexture2D<float4>& GetColor() { return color; }
			float& GetStreak_intensity() { return streak_intensity; }
			uint& GetUse_streaks() { return use_streaks; }
			static constexpr SIG_TYPE TYPE = SIG_TYPE::Table;
			template<class Compiler>
			void compile(Compiler& compiler) const
			{
				compiler.compile(streak_intensity, "LensFlareComposite::streak_intensity");
				compiler.compile(use_streaks, "LensFlareComposite::use_streaks");
				compiler.compile(ghosts, "LensFlareComposite::ghosts");
				compiler.compile(streaks, "LensFlareComposite::streaks");
				compiler.compile(color, "LensFlareComposite::color");
			}
			struct Compiled
			{
				float streak_intensity; // float
				uint use_streaks; // uint
				uint ghosts; // Texture2D<float4>
				uint streaks; // Texture2D<float4>
				uint color; // RWTexture2D<float4>

			
				private:
				SERIALIZE()
				{
					ar& NVP(streak_intensity);
					ar& NVP(use_streaks);
				}


			};

			static std::string get_typename()
			{
				return "Tables::LensFlareComposite";
			}
			private:
			SERIALIZE()
			{
				ar& NVP(streak_intensity);
				ar& NVP(use_streaks);
			}

		};
	}
	#pragma pack(pop)
}

