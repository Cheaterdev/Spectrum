// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by Prism from .prism files in sources/Prism/defs/
// Changes will be lost on next generation. Edit the .prism source files instead.
// ============================================================================
export module HAL:Autogen.Tables.Post.BloomUpsample;

import Core;
import :SIG;
import :Types;
import :HLSL;
import :Enums;
export namespace Table
{
	#pragma pack(push, 1)
	namespace Post
	{
		struct BloomUpsample
		{
			static constexpr SlotID ID = SlotID::BloomUpsample;
			float2 low_texel;
			float radius;
			::Post::BloomMode mode;
			float scatter;
			HLSL::Texture2D<float4> low;
			HLSL::Texture2D<float4> high;
			HLSL::RWTexture2D<float4> target;
			HLSL::Texture2D<float4>& GetLow() { return low; }
			HLSL::Texture2D<float4>& GetHigh() { return high; }
			HLSL::RWTexture2D<float4>& GetTarget() { return target; }
			float2& GetLow_texel() { return low_texel; }
			float& GetRadius() { return radius; }
			::Post::BloomMode& GetMode() { return mode; }
			float& GetScatter() { return scatter; }
			static constexpr SIG_TYPE TYPE = SIG_TYPE::Table;
			template<class Compiler>
			void compile(Compiler& compiler) const
			{
				compiler.compile(low_texel, "BloomUpsample::low_texel");
				compiler.compile(radius, "BloomUpsample::radius");
				compiler.compile(mode, "BloomUpsample::mode");
				compiler.compile(scatter, "BloomUpsample::scatter");
				compiler.compile(low, "BloomUpsample::low");
				compiler.compile(high, "BloomUpsample::high");
				compiler.compile(target, "BloomUpsample::target");
			}
			struct Compiled
			{
				float2 low_texel; // float2
				float radius; // float
				::Post::BloomMode mode; // BloomMode
				float scatter; // float
				uint low; // Texture2D<float4>
				uint high; // Texture2D<float4>
				uint target; // RWTexture2D<float4>

			
				private:
				SERIALIZE()
				{
					ar& NVP(low_texel);
					ar& NVP(radius);
					ar& NVP(mode);
					ar& NVP(scatter);
				}


			};

			static std::string get_typename()
			{
				return "Tables::BloomUpsample";
			}
			private:
			SERIALIZE()
			{
				ar& NVP(low_texel);
				ar& NVP(radius);
				ar& NVP(mode);
				ar& NVP(scatter);
			}

		};
	}
	#pragma pack(pop)
}

