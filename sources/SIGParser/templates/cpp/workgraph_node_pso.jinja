// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by SigParser from .sig files in sources/SIGParser/sigs/
// Changes will be lost on next generation. Edit the .sig source files instead.
// ============================================================================
export module HAL:Autogen.PSO.{{pso.name}}_{{node.name}};

import Core;
import :PSO;
import :Enums;
import :Types;


export namespace PSOS
{
	struct {{pso.name}}_{{node.name}}: public PSOBase
	{
		struct Keys {
			GEN_DEF_COMP(Keys);
		private:
			SERIALIZE()
			{
			}
		};

		GEN_COMPUTE_PSO({{pso.name}}_{{node.name}})


		SimplePSO init_pso(Keys & key, std::function<void(SimplePSO&, Keys&)> f)
		{

			SimplePSO mpso("{{pso.name}}_{{node.name}}");
			if(f) f(mpso,key);

			mpso.root_signature = Layouts::{{pso.root_sig.name}};
{% for v in (pso.shaders|list) %}
{%- set shader = pso.shaders[v]-%}
			mpso.compute.file_name = "shaders/{{shader.path}}.hlsl";
{%- endfor %}
			mpso.compute.entry_point = "{{node.name}}";
			mpso.compute.flags = HAL::ShaderOptions::None;
			mpso.compute.macros.emplace_back("WORKGRAPH_EMULATION", "1");

			return mpso;
		}

		private:
		SERIALIZE()
		{
			ar&NVP(wrap(psos));
		}
	};
}
