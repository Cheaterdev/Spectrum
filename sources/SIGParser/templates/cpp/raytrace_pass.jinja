// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by SigParser from .sig files in sources/SIGParser/sigs/
// Changes will be lost on next generation. Edit the .sig source files instead.
// ============================================================================
#pragma once

struct {{pso.name}}: public RaytracePass<{{pso.name}}>
{
	using Payload = Table::{{pso.params['payload'].expr}};
{% if pso.params['local'] is defined %}
	using LocalData =  Slots::{{pso.params['local'].expr}};
{% endif %}

	static const constexpr uint ID = {{ pso.index }};

	static const constexpr std::string_view shader = "shaders\\{{pso.shaders['miss'].path}}.hlsl";
	static const constexpr std::wstring_view name = L"{{pso.name}}_GROUP";
	static const constexpr std::wstring_view hit_name = L"{{pso.shaders['closest_hit'].options['EntryPoint'].value_atom.expr}}";
	static const constexpr std::wstring_view miss_name = L"{{pso.shaders['miss'].options['EntryPoint'].value_atom.expr}}";
	// Empty when this pass doesn't declare an any_hit stage -- always emitted
	// (rather than conditionally, per pass) so RaytracePass<Desc>'s init()/
	// init_for_material() can check Desc::any_hit_name.empty() uniformly
	// instead of needing per-Desc SFINAE for a member that may not exist.
	static const constexpr std::wstring_view any_hit_name = L"{% if pso.shaders['any_hit'] is defined %}{{pso.shaders['any_hit'].options['EntryPoint'].value_atom.expr}}{% endif %}";

	static const constexpr bool per_material = {{pso.params['per_material'].expr or false}};
};