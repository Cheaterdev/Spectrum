#pragma once

struct {{rt.name}}
{
{%- for e in rt.values -%}
{%- if starts_with(e.class_no_template,"RenderTarget")%}
    {{e.template_arg}} {{e.name}}: SV_Target{{loop.index0}};
{%- endif -%}
{%- endfor %}
};