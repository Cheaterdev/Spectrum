#pragma once

struct {{rt.name}}
{
{%- for e in rt.values -%}
{%- if e.class_no_template is startsWith('RenderTarget')%}
    {{e.template_arg}} {{e.name}}: SV_Target{{loop.index0}};
{%- endif -%}
{%- endfor %}
};