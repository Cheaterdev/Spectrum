// ============================================================================
// THIS FILE IS AUTOMATICALLY GENERATED - DO NOT MANUALLY EDIT
// ============================================================================
// Generated by SigParser from .sig files in sources/SIGParser/sigs/
// Changes will be lost on next generation. Edit the .sig source files instead.
// ============================================================================
{#- Not #pragma once: enums.h is reached through several different relative
    include chains (workgraph headers reach it via a "../tables/..enums.h"-
    style path, table headers reach it directly), and the shader compiler's
    #pragma-once dedup keys off that unnormalized resolved path string, not
    real file identity -- two different chains reaching the same file both
    "win" and the enum gets redefined. A macro guard doesn't have this
    problem: it's keyed on preprocessor state, not path spelling. Matches the
    existing sig_hlsl.hlsl convention for the same reason. #}
#ifndef SIG_ENUMS_H
#define SIG_ENUMS_H

{%- for e in parsed.enums %}
{%- set enum = parsed.enums[e] %}
enum class {{enum.name}} : uint
{
	{%- for v in enum.values %}
	{{v.name}}{% if v.expr %} = {{v.expr}}{% endif %}{% if not loop.last %},{% endif %}
	{%- endfor %}
};
{%- endfor %}

#endif
