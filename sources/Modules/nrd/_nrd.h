#pragma once
// NRD.h is self-contained (it includes NRDDescs.h/NRDSettings.h itself) --
// this aggregating header exists only for parity with the other Modules
// wrappers (streamline, cereal), which need one for headers that aren't.
#include <nrd/NRD.h>
